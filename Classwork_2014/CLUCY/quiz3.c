#include <stdio.h>
int main ()
{
   int  var1;
   char var2[10];

   printf("This is going to be the address of the integer: %x\n", &var1  );
   printf("The address of the variable will be the character data type yahoo: %x\n", &var2  );

     getch();
     system ("PAUSE"); 
   return 0;
}

